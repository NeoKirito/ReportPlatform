# 只需修改这两个配置；修改后先关闭服务，再启动服务。
# 默认监听本机全部 IPv4 网卡的 82 端口。
Port=82

# 填写现有 SQL Server 报表数据库连接字符串（只在此文件保存）。
# 示例：Server=192.168.0.10;Database=TJXT3.0;User ID=report_user;Password="your-password";Encrypt=True;TrustServerCertificate=True;
# 如需严格校验证书，使用受信任的 SQL Server 证书并设 TrustServerCertificate=False。
# 密码包含分号时请用双引号包住密码；反斜杠、百分号无需 CMD/JSON 转义。
ConnectionString=Server=192.168.0.237;Database=TJXT0616;User ID=sa;Password=Sxyckj#123;TrustServerCertificate=True;
